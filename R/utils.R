format_numbers <- function(x) { formatC(x,
                                        digits = 0,
                                        format = 'f',
                                        big.mark = ',')
}